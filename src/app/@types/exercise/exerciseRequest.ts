export type RequestExercise = {
    text: string,
    translation: string,
    audio_url: string,
    observation: string
}

export type RequestUpdateExercise = {
    text: string,
    translation: string,
    observation: string
}
